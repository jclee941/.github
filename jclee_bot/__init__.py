"""jclee_bot — fork-owned extensions for the jclee-bot GitHub App.

This package is OWNED by the fork (jclee941/.github) and is intentionally
separate from the upstream ``pr_agent`` package, which must not be modified
(see pr_agent/AGENTS.md). It adds an App Checks-API runner so the GitHub App
can run lightweight static checks on PRs and report them via the Checks API
without requiring per-repo workflow files.
"""
